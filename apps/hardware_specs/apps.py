from django.apps import AppConfig

class HardwareSpecsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.hardware_specs'  # <-- Change this from 'hardware_specs'